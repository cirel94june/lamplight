PORT=8787
OWNER_TOKEN=change-me-to-a-random-string
DATABASE_URL=file:lamplight.db

# API key encryption — required, generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
