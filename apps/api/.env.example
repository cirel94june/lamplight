PORT=8787
OWNER_TOKEN=change-me-to-a-random-string
DATABASE_URL=file:lamplight.db

# AI Gateway (optional — provider is only registered if key is set)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# API key encryption — required for DB-stored providers, generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
